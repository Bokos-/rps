/**************************************************
** GAME PLAYER CLASS
**************************************************/

var Warrior = function(x, y, weapon) {
	
	var x;
	var y;
	var weapon = weapon;

	// Define which variables and methods can be accessed
	return {
		x: x,
		y: y,
		weapon: weapon
	}
};